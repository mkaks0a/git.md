## 期末テストに向けて 目指せ100点
point
1,外部ツールをマスターする。
2,dart
3,
4,

## 今日の授業内容
dartpadをする。
演習1 print関数を使う
演習2 演算子をprint関数を使用して出力する。

比較演算子によって真偽値が分かるとすごい便利になる。
演習3 条件分岐 yearに数字を保存してうるう年かを判定入れ子(ネスト)  関数{関数{}}
void main() {
  int year = 4200;
  if (year % 400 != 0 && year % 100 == 0) {
    print("うるう年ではない");
  } else if (year % 100 == 0) {
    print("うるう年です");
  } else if (year % 4 == 0) {
    print("うるう年です");
  } else {
    print("うるう年ではない");
  }
}

演習4 適切な機能(関数)の分割を使う。
1メゾット(関数)=1機能
age=28(年齢) year=2026(現在の西暦) 生まれた年はうるう年？
                                                        次回に続く

## 分かった事
      演習4の機能分割が難しかったです。簡単な処理の内容でも行数が多くなることが分かりました。
    return の扱いに戸惑っている事に気が付いたので要意識する。